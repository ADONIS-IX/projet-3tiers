apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: reseau-dmz
  namespace: projet-3tiers
  annotations:
    description: "Réseau DMZ — Serveur Web accessible depuis Internet (192.168.100.0/24)"
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "reseau-dmz",
      "type": "bridge",
      "bridge": "br-dmz",
      "ipam": {
        "type": "static"
      }
    }
